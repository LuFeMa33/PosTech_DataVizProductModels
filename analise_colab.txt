# -*- coding: utf-8 -*-
"""analise.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1qDMouZsChoUjoxR1xtZpX1kydQJVD3Gm
"""

import pandas as pd

pd.set_option('display.max_columns', None)

df_obesidade = pd.read_csv('https://raw.githubusercontent.com/LuFeMa33/PosTech_DataVizProductModels/refs/heads/main/Obesity.csv')

df_obesidade.head()

df_obesidade.shape

df_obesidade.info()

dict_colunas_alteracao = {'Gender':'Genero',
                          'Age':'Idade',
                          'Height':'Altura',
                          'Weight':'Peso',
                          'family_history':'historico_familiar',
                          'FAVC':'frequencia_consumo_alta_caloria',
                          'FCVC':'frequencia_consumo_vegetais',
                          'NCP':'numero_refeicoes_principais',
                          'CAEC':'consumo_lanches_entre_refeicoes',
                          'SMOKE':'fumante',
                          'CH2O':'consumo_agua',
                          'SCC':'monitoramento_consumo_calorias',
                          'FAF':'frequencia_atividade_fisica',
                          'TUE':'tempo_uso_dispositivos_eletronicos',
                          'CALC':'consumo_alcool',
                          'MTRANS':'meio_transporte',
                          'Obesity':'Obesidade'}

df_obesidade.rename(columns=dict_colunas_alteracao, inplace=True)

df_obesidade.head()

df_obesidade['historico_familiar'] = df_obesidade['historico_familiar'].map({'yes': '1', 'no': '0'})
df_obesidade['frequencia_consumo_alta_caloria'] = df_obesidade['frequencia_consumo_alta_caloria'].map({'yes': '1', 'no': '0'})
df_obesidade['fumante'] = df_obesidade['fumante'].map({'yes': '1', 'no': '0'})
df_obesidade['monitoramento_consumo_calorias'] = df_obesidade['monitoramento_consumo_calorias'].map({'yes': '1', 'no': '0'})
df_obesidade['Indice_Genero'] = df_obesidade['Genero'].map({'Female':0, 'Male':1})

df_obesidade.head()

set(df_obesidade['Obesidade'])

set(df_obesidade['meio_transporte'])

set(df_obesidade['consumo_lanches_entre_refeicoes'])

df_obesidade.head()

df_obesidade['IMC'] = (df_obesidade['Peso'] / (df_obesidade['Altura'])**2).round(3)

df_obesidade.head()

df_obesidade.head()

df_obesidade.info()

df_obesidade['historico_familiar'] = df_obesidade['historico_familiar'].astype('int64')
df_obesidade['frequencia_consumo_alta_caloria'] = df_obesidade['frequencia_consumo_alta_caloria'].astype('int64')
df_obesidade['fumante'] = df_obesidade['fumante'].astype('int64')
df_obesidade['monitoramento_consumo_calorias'] = df_obesidade['monitoramento_consumo_calorias'].astype('int64')

df_obesidade.info()

df_obesidade.describe()

df_obesidade.isnull().sum()

df_obesidade.drop(columns=['Genero'], inplace=True)

import matplotlib.pyplot as plt
import seaborn as sns

corr = df_obesidade.corr(numeric_only=True)
plt.figure(figsize=(15,10))
sns.heatmap(corr, cmap='Blues', annot=True)
plt.show()

colunas_onehot = [
    'consumo_lanches_entre_refeicoes',
    'consumo_alcool',
    'meio_transporte'
]

df_obesidade = pd.get_dummies(
    df_obesidade,
    columns=colunas_onehot,
    drop_first=True,
    dtype=int
)

df_obesidade.head()

from sklearn.preprocessing import LabelEncoder

#df_obesidade['Obesidade_encoder'] = LabelEncoder().fit_transform(df_obesidade['Obesidade'])

#df_obesidade.groupby('Obesidade_encoder')['Obesidade'].unique().reset_index()

df_obesidade['Obesidade'] = LabelEncoder().fit_transform(df_obesidade['Obesidade'])

df_obesidade.head()

set(df_obesidade['Obesidade'])

corr_2 = df_obesidade.corr(numeric_only=True)
plt.figure(figsize=(20,15))
sns.heatmap(corr_2, cmap='Blues', annot=True)
plt.show()

plt.figure(figsize=(18, 14))

corr = df_obesidade.corr(numeric_only=True)

sns.heatmap(
    corr,
    cmap='coolwarm',
    center=0,
    annot=True,
    fmt=".2f",
    linewidths=0.5
)

plt.title("Matriz de Correlação")
plt.show()

df_obesidade.corr()['Obesidade'].sort_values(ascending=False)

df_obesidade.to_csv('Obesidade_tratado.csv', index=False)

df_obesidade.describe().T

df_obesidade['Obesidade'].value_counts().sort_index().reset_index()

colunas = [
    'Idade',
    'Altura',
    'Peso',
    'IMC',
    'consumo_agua',
    'frequencia_atividade_fisica',
    'tempo_uso_dispositivos_eletronicos'
]

df_obesidade[colunas].hist(figsize=(15,10))

plt.tight_layout()
plt.show()

for coluna in [
    'Idade',
    'Peso',
    'Altura',
    'IMC'
]:
    plt.figure(figsize=(6,2))

    sns.boxplot(x=df_obesidade[coluna])

    plt.title(coluna)

    plt.show()

df_obesidade.columns.tolist()

sns.boxplot(
    x='Obesidade',
    y='frequencia_atividade_fisica',
    data=df_obesidade
)

sns.boxplot(
    x='Obesidade',
    y='frequencia_consumo_alta_caloria',
    data=df_obesidade
)

sns.boxplot(
    x='Obesidade',
    y='frequencia_consumo_vegetais',
    data=df_obesidade
)

df_obesidade_sem_imc = df_obesidade.drop(columns=['IMC'])

df_obesidade_sem_imc.head()

df_obesidade_sem_imc.columns.to_list()

df_obesidade_com_imc = df_obesidade.copy()

from sklearn.linear_model import LinearRegression

lr = LinearRegression()

from google.colab import drive
drive.mount('/content/drive')

X = df_obesidade_sem_imc[['Idade',
 'Altura',
 'Peso',
 'historico_familiar',
 'frequencia_consumo_alta_caloria',
 'frequencia_consumo_vegetais',
 'numero_refeicoes_principais',
 'fumante',
 'consumo_agua',
 'monitoramento_consumo_calorias',
 'frequencia_atividade_fisica',
 'tempo_uso_dispositivos_eletronicos',
 'Indice_Genero',
 'consumo_lanches_entre_refeicoes_Frequently',
 'consumo_lanches_entre_refeicoes_Sometimes',
 'consumo_lanches_entre_refeicoes_no',
 'consumo_alcool_Frequently',
 'consumo_alcool_Sometimes',
 'consumo_alcool_no',
 'meio_transporte_Bike',
 'meio_transporte_Motorbike',
 'meio_transporte_Public_Transportation',
 'meio_transporte_Walking']]

y = df_obesidade_sem_imc['Obesidade']

from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state = 101)

lr.fit(X_train, y_train)

y_pred = lr.predict(X_test)

fig = plt.figure(figsize=(8, 5), dpi=80)
plt.rcParams.update({'font.size': 14})
ax = sns.regplot(x = y_test, y = y_pred)
ax.set(xlabel='B real', ylabel='B predito')
ax = plt.plot(y_test, y_test, '--r')
plt.show()

from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

MAE = mean_absolute_error(y_test, y_pred)
MSE = mean_squared_error(y_test, y_pred)
r2 = r2_score(y_test, y_pred)

print('MAE', MAE)
print('MSE', MSE)
print('r²', r2)

from sklearn.tree import DecisionTreeRegressor

model_dtr = DecisionTreeRegressor(random_state=101, max_depth=10)
model_dtr.fit(X_train, y_train)

y_pred_model_dtr = model_dtr.predict(X_test)

MAE = mean_absolute_error(y_test, y_pred_model_dtr)
MSE = mean_squared_error(y_test, y_pred_model_dtr)
r2 = r2_score(y_test, y_pred_model_dtr)

print('MAE', MAE)
print('MSE', MSE)
print('r²', r2)

fig = plt.figure(figsize=(8, 5), dpi=80)
plt.rcParams.update({'font.size': 14})
ax = sns.regplot(x = y_test, y = y_pred_model_dtr)
ax.set(xlabel='y real', ylabel='y predito')
ax = plt.plot(y_test, y_test, '--r')
plt.show()

from sklearn.tree import DecisionTreeClassifier

model_dtc = DecisionTreeClassifier(
    random_state=101,
    max_depth=10
)

model_dtc.fit(X_train, y_train)

y_pred_model_dtc = model_dtc.predict(X_test)

from sklearn.metrics import accuracy_score

accuracy = accuracy_score(y_test, y_pred_model_dtc)

print(f'Acurácia: {accuracy:.2%}')

from sklearn.neighbors import KNeighborsClassifier

knn = KNeighborsClassifier(n_neighbors=3)

knn.fit(X_train, y_train)

y_pred_knn = knn.predict(X_test)

accuracy = knn.score(X_test, y_test)
print(accuracy)

from sklearn.metrics import confusion_matrix

cm = confusion_matrix(y_test, y_pred_model_dtc)

plt.figure(figsize=(9, 7))

sns.heatmap(
    cm,
    annot=True,
    fmt='d',
    cmap='Blues',
    xticklabels=sorted(y_test.unique()),
    yticklabels=sorted(y_test.unique())
)

plt.xlabel('Classe prevista')
plt.ylabel('Classe real')
plt.title('Matriz de Confusão - Decision Tree Classifier')

plt.show()

from sklearn.metrics import classification_report

print(classification_report(y_test, y_pred_knn))

cm_knn = confusion_matrix(y_test, y_pred_knn)

plt.figure(figsize=(8, 6))

sns.heatmap(
    cm_knn,
    annot=True,
    fmt='d',
    cmap='Blues',
    xticklabels=sorted(y_test.unique()),
    yticklabels=sorted(y_test.unique())
)

plt.xlabel('Classe prevista')
plt.ylabel('Classe real')
plt.title('Matriz de Confusão - KNN')

plt.show()

from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()

X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)

model_knn = KNeighborsClassifier(n_neighbors=5)

model_knn.fit(X_train_scaled, y_train)

y_pred_knn = model_knn.predict(X_test_scaled)

from sklearn.metrics import accuracy_score

accuracy_knn = accuracy_score(y_test, y_pred_knn)

print(f'Acurácia: {accuracy_knn:.2%}')

print(classification_report(y_test, y_pred_knn))

from sklearn.ensemble import GradientBoostingClassifier

modelo_xgb = GradientBoostingClassifier()

modelo_xgb.fit(X_train, y_train)

y_pred_gbc = modelo_xgb.predict(X_test)

accuracy_gbc = accuracy_score(y_test, y_pred_gbc)

print(f'Acurácia: {accuracy_gbc:.2%}')

print(
    classification_report(
        y_test,
        y_pred_gbc
    )
)

cm_gbc = confusion_matrix(y_test, y_pred_gbc)

plt.figure(figsize=(9, 7))

sns.heatmap(
    cm_gbc,
    annot=True,
    fmt='d',
    cmap='Blues',
    xticklabels=sorted(y_test.unique()),
    yticklabels=sorted(y_test.unique())
)

plt.xlabel('Classe prevista')
plt.ylabel('Classe real')
plt.title('Matriz de Confusão - Gradient Boosting')

plt.show()

importancias = pd.Series(
    modelo_xgb.feature_importances_,
    index=X_train.columns
).sort_values(ascending=False)

print(importancias)

importancias.head(15).sort_values().plot(
    kind='barh',
    figsize=(10, 6)
)

plt.title('Top 15 - Importância das Variáveis')
plt.xlabel('Importância')
plt.show()

import joblib

joblib.dump(modelo_xgb, 'modelo_xgb.joblib')

joblib.dump(X_train.columns.tolist(), 'colunas_modelo.joblib')

print(X_train.columns.tolist())

print(type(modelo_xgb))

joblib.dump(modelo_xgb, 'modelo_gradient_boosting.joblib')

import sklearn
import joblib
import numpy

print("Python:", __import__('sys').version)
print("Scikit-learn:", sklearn.__version__)
print("Joblib:", joblib.__version__)
print("NumPy:", numpy.__version__)